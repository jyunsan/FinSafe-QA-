# FinSafe-QA 市场调研与产品规划

[TOC]

---

## 1. 产品定位

FinSafe-QA 是一款面向金融投研团队的 **AI 智能问数助手**，核心链路为：**自然语言 → SQL → 结构化数据 → 自然语言回复**。

与通用大模型的根本区别：大模型在此处于**受控链路**中——负责语义理解和 SQL 生成，但每条 SQL 经过语法校验、语义校验、结果合理性校验三道关卡，每个数据结果都能追溯到源表和源字段。

一句话定义：**不做"能回答问题"的系统，只做"回答错了能被发现、回答对了能被证明"的系统。**

MVP 阶段聚焦中药上市公司财报分析，后续逐步扩展至全 A 股及多资产类别。

---

## 2. 目标用户

| 用户角色 | 核心诉求 | 使用频率 | 当前工作方式 |
|:---|:---|:---|:---|
| **初级研究员** | 快速查单指标、单公司财务数据，替代手动翻财报和 Excel 整理 | 高频（日均 10-30 次） | 手动下载财报 PDF → 定位报表 → 复制数据 → 整理表格 |
| **资深分析师** | 跨公司对比、多期趋势分析、归因解释，需验证数据来源和计算逻辑 | 中频（日均 5-10 次） | SQL 查询 / Wind 终端 + Excel 建模 + 研报阅读 |
| **合规/风控人员** | 审计 AI 查询链路，确保数据可追溯、无合规风险 | 低频（月度/季度） | 无 AI 审计工具，依赖人工抽查 |

**现阶段不服务**：散户个人投资者（缺少机构合规需求驱动）、量化策略研究员（需要实时行情和回测能力，超出产品范围）。

---

## 3. 用户痛点

用户的核心痛点不是"不会查财报"，而是**财报分析链路长、多源信息割裂、查询门槛高，且 AI 生成结果缺少可信保障**。

| 用户痛点 | 具体表现 | 产品需要解决的问题 |
|:---|:---|:---|
| 财报数据获取链路长 | 用户需下载财报、定位报表、复制字段、整理表格后才能分析 | 用自然语言替代手动查数，降低获取和整理成本 |
| 跨公司、跨周期对比成本高 | 多家公司、多报告期、多指标对比需反复查找和计算 | 支持自然语言完成趋势、排名和对比分析 |
| 财务指标和企业名称口径复杂 | 用户表达包含简称、股票代码、模糊指标或不完整术语 | 做好实体归一、指标映射和意图澄清 |
| 财务数据与研报解释割裂 | 数值在财报/数据库中，变动原因在研报、公告或政策资料里 | 打通结构化数据查询和 RAG 归因 |
| AI 回答可信度不足 | 大模型可能出现字段幻觉、数值编造、无依据归因或错误结论 | 引入语法/语义/合理性三级校验、全链路溯源和分级兜底 |
| SQL 查询门槛高 | 投研人员通常不具备 SQL 能力，但需要灵活的数据查询 | 用 NL→SQL 替代人工编写查询 |

---

## 4. 为什么需要大模型

该场景适合使用大模型，核心原因是用户问题以**自然语言、口语化且不完整**的方式提出，传统规则系统或固定 BI 报表无法覆盖"查数、对比、追问、归因"连续发生的真实投研流程。

大模型在本产品中承担以下不可替代的角色：

| 能力 | 为什么传统方案做不到 | 大模型的作用 |
|:---|:---|:---|
| **语义理解** | 规则匹配无法覆盖"茅台去年赚了多少""600519 Q1 盈利能力"等口语化变体 | 识别公司、指标、时间、分析动作和隐含意图 |
| **SQL 生成** | 固定 BI 报表只能查预设指标组合，无法响应灵活的分析问题 | 根据自然语言意图自动生成查询 SQL |
| **任务规划** | 复合问题（"对比 A 和 B 的 ROE 趋势，找出差异最大的季度"）需拆解为多子任务 | 拆解复杂问题并编排执行顺序 |
| **分析生成** | 规则模板只能输出固定格式，无法生成有洞察的文字分析 | 基于查询结果生成自然的分析文字 |

**但大模型不是全部**：结构化财务数据负责保证数值准确，规则引擎负责校验 SQL 正确性，审计日志负责全链路可追溯。大模型是链路中的"理解和生成引擎"，不是"最终决策者"。

---

## 5. 市场调研：用户最看重什么

### 5.1 调研方法

综合 2025-2026 年行业标准文件（T/GXDSL 036—2025）、公开竞品分析（WarrenQ、WindClaw/Alice、Bloomberg AI）、学术基准（FINCH Benchmark、BIRD-Bench）、金融 AI 落地实践报告及行业圆桌讨论，形成以下结论。

### 5.2 用户需求优先级

市场调研结论高度一致——金融 B 端用户对 AI 问数产品的核心诉求按重要性排序如下：

#### 🥇 第一梯队：决定采购决策

##### 5.2.1 准确性

金融行业对数据错误的容忍度极低。行业标准（T/GXDSL 036—2025）明确了量化边界：

| 场景 | 行业标准 | 对产品的含义 |
|:---|:---|:---|
| 交易金额 | 误差 ≤ 0.01 元 | 财务数值不能有任何计算偏差 |
| 征信/工商数据 | 误差率 ≤ 0.5%，一致性 ≥ 98% | 公司实体和财务指标映射必须精确 |
| 数据分析查询 | 复杂查询准确率需 ≥ 90% | 可落地的下限，低于此值客户不可接受 |

**行业现状数据**：

- 未经优化的基础模型，金融场景首次 SQL 生成正确率仅 **43%-58%**（某金融企业实测）
- 经过 RAG + 领域微调 + 自我修正等多维优化后，可提升至 **78%-91%**（某银行 6 个月优化实践）
- 行业最优水平：蚂蚁 Agentar-SQL 在 BIRD 基准 81.67%，银行 POC 达 **92%**
- 但 SPIDER2 基准揭示：GPT-4o 从通用场景 86.6% 跌至真实企业工作流 **10.1%** — 说明真实场景远比学术基准难

**核心洞察**：

> 行业共识：不要报"准确率 95%"这种单一数字。必须按查询复杂度分层——固定口径查询可达 99%+，但开放式复杂查询 60-80% 才是现实。向客户诚实展示这一边界，比报一个虚假的高数字更能建立长期信任。

竞品对比：
- WarrenQ 公布"取数准确率 95%+"— 但这是 NL2API 路线下的固定 API 调用准确率，不等于开放式 SQL 生成准确率
- WindClaw **未公布准确率数字** — 转而强调"每个结论背后的数据源与逻辑链路可核验"
- 本产品策略：**分层公开** L1（模板命中）≥ 99%、L2（模型生成）≥ 90%、L3/L4 坦诚标注低置信

##### 5.2.2 稳定性

**这是最被低估却最致命的问题。** 行业实践揭示：

- 同一台服务器仅扩展 1GB 内存，相同模型和提示词，输出结果都可能不同
- "跑得通但结果不对"的语义错误最难检测，可能造成千万级营收数据失真
- 金融分析师的真实原话："我宁可用一个 60 分但每次都一样的东西，也不用一个 90 分但每次不一样的东西"

行业应对策略对比：

| 厂商 | 稳定性策略 | 本质 |
|:---|:---|:---|
| WarrenQ | NL2API 封装，API 的入参出参是确定的 | **绕过生成**，不让模型写 SQL |
| WindClaw | MCP 工具封装 + 本地化部署 + 模型版本锁定 | **隐藏 SQL**，工具有固定契约 |
| Eventus Frank AI | "确定性 AI"路线，确保安全、可重复、透明 | **放弃生成式**，回退到确定性系统 |

WarrenQ 选择 NL2API 而非 NL2SQL，本身就是对"大模型生成 SQL 不够稳定"这一事实的承认。

**对本产品的含义**：NL→SQL 路线天然面临比 NL→API 更大的稳定性挑战。必须建立补偿机制 — 温度参数锁定（temperature = 0）、SQL 语义等价性校验、高频查询模板缓存、模型版本锁定的全量回归测试套件。

##### 5.2.3 可解释性与可审计性

行业标准（T/GXDSL 036—2025）已将其从"加分项"提升为**准入门槛**：

| 要求 | 标准 |
|:---|:---|
| Top10 特征解释力 | ≥ 75% |
| 非技术人员理解测试准确率 | ≥ 90% |
| 数据处理链路可视化度 | **100%** |
| 审计日志留存 | ≥ 180 天 |
| 变更记录留存 | ≥ 3 年 |

Bloomberg、Deloitte 的金融 AI 产品均将"展示推理过程 + 源引用 + 置信度评分"作为**默认功能**而非增值功能。ALFIN（GFTN）在不确定时直接展示置信度，Frank AI（Eventus）提供全审计追踪。

竞品在这一维度的做法：

| 厂商 | 可回溯能力 | 可审计程度 |
|:---|:---|:---|
| WarrenQ | 链路打印 + 取值追踪 + 引用溯源 + 白盒路径调试 | 开发者级别，可在线调试 Agent 行为 |
| WindClaw | 数据源保留 + 逻辑链路保留 + 阶段校验点记录 | 每个结论可核验、可复盘 |
| **本产品目标** | SQL 完全可见 + 源表.字段级血统 + 置信度三色标签 + 16 字段审计日志 | **全透明**，超过所有竞品 |

#### 🥈 第二梯队：决定使用体验

##### 5.2.4 数据安全与合规

- 敏感字段脱敏率 **100%**
- 必须支持**私有化部署**（数据不出企业内网）
- 买方机构持仓数据和研究观点是核心商业机密，任何外泄风险零容忍
- 数据库连接**只读权限**，禁止写入/修改/删除操作

##### 5.2.5 响应延迟

- 简单查询端到端 ≤ **3 秒**（超过此值用户宁愿自己查）
- 复杂多表分析 ≤ **8 秒**
- 深度报告生成可放宽至分钟级

参考：FinStat2SQL 论文将"次 4 秒响应"作为生产可接受延迟的基准线。

##### 5.2.6 领域覆盖深度

投研场景的特殊要求：财务三表（BS/IS/CF）联动分析、时间序列智能识别（QoQ / YoY / MTD / YTD）、行业分类体系映射（申万 / 中信 / GICS）、多币种与多会计准则处理、复权/除权等中国 A 股特色数据逻辑。

#### 🥉 第三梯队：决定竞争差异

##### 5.2.7 多轮交互与追问

分析师的真实工作流：第一问看概况 → 追问下钻 → 交叉验证 → 归因分析。系统需理解上下文指代和隐式约束。WarrenQ 和 WindClaw 均已支持，但成熟度参差不齐。

##### 5.2.8 数据底座完整性

当前行业规律：**"带数据进组"的厂商才有议价权**。万得（30 年数据积累）和恒生聚源（20 年积累）的护城河不在于 AI 能力，而在于数据覆盖度和权威性。纯算法厂商在深水区会越来越弱势。

##### 5.2.9 生态整合

Excel/PPT 一键导出（投研标配工作流）、与 O32/衡泰等投资管理系统打通、MCP 协议支持（同花顺已上线 iFinD MCP）。

### 5.3 竞品关键差距与差异化机会

| 维度 | WarrenQ（恒生聚源） | WindClaw/Alice（万得） | 本产品差异化 |
|:---|:---|:---|:---|
| 技术路线 | NL2API（绕开 SQL 生成） | MCP 工具封装（隐藏 SQL） | **NL2SQL 全透明**，SQL 完全可审计 |
| 准确率公开 | 95%+（API 取数） | 未公布 | **分层公开**，L1 ≥ 99%、L2 ≥ 90%，L3/L4 坦诚标注 |
| SQL 可见性 | 不可见（封装在 API 内） | 不可见（封装在 MCP 工具内） | **每条 SQL 用户可查看** |
| 数据溯源粒度 | 取值追踪 + 链路打印 | 数据源 + 逻辑链路 | **源表.字段级别完整血统** |
| 私有化部署 | 支持 | 支持（零代码） | 必须具备 |
| 定价模式 | 弹性定价（SaaS 低至 4000 元/年/账号，私有化高价） | 机构级高价 + 个人版探索 | — |

**核心空白点**：没有任何竞品敢把原始 SQL 直接暴露给客户审计。WarrenQ 用 API 封装绕开，WindClaw 用 MCP 工具隐藏。**本产品以"全透明"建立机构信任** — 这既是差异化，也是定价权的来源。

---

## 6. 产品计划（三阶段）

### Phase 1：可信问数

**时间**：当前阶段

**目标**：让客户信任系统的输出。不做复杂功能，只证明"查得准、查得稳、查得可追溯"。

**核心交付**：

- **准确性**：L1 固定口径查询（模板匹配，≥ 99%）、L2 单域分析查询（模型生成 + 三重校验，≥ 90%）、三级校验管线（语法 → 语义 → 结果合理性）
- **稳定性**：temperature = 0、模型版本锁定、语义等价性测试、边界扰动测试、查询可复现性保障
- **可回溯**：SQL 完全可见、数据血统追溯（源表.字段级）、置信度三色标签（🟢🟡🔴）、全链路审计日志（16 字段，≥ 180 天留存）
- **基础设施**：私有化部署能力、数据库只读权限、敏感信息脱敏

**明确不做**：L3 跨域复杂查询、L4 深度分析、RAG 研报归因、多轮对话、可视化图表、多数据源交叉验证

**详细 PRD 见**：[07 Phase1-MVP-PRD.md](07%20Phase1-MVP-PRD.md)

---

### Phase 2：可信分析

**时间**：Phase 1 达标后启动

**准入条件**：Phase 1 L1 准确率 ≥ 99%、L2 准确率 ≥ 90% 持续稳定 ≥ 1 个月

**目标**：从"查数"扩展到"理解数据"，增加分析深度和覆盖广度。

**核心交付**：

| 能力 | 说明 |
|:---|:---|
| L3 跨域复杂查询 | 多表 JOIN、子查询、窗口函数，目标准确率 ≥ 75% |
| RAG 研报归因 | 检索研报/公告/政策资料，为数据分析提供原因解释 |
| 多轮对话 | 上下文继承、指代消解、追问下钻 |
| 基础可视化 | 表格、趋势图、对比图自动生成 |
| Bad Case 自动回流 | 用户反馈标记的错误自动进入回归测试集 |

**不做**：完全自动化的深度研报生成、投资策略推荐、实时行情分析

---

### Phase 3：可信推理

**时间**：Phase 2 达标后启动

**准入条件**：Phase 2 L3 准确率 ≥ 75%、RAG 证据有效率 ≥ 70% 持续稳定 ≥ 2 个月

**目标**：从工具升级为投研工作平台，支持完整的"查数 → 归因 → 建模 → 报告"工作流。

**核心交付**：

| 能力 | 说明 |
|:---|:---|
| L4 深度分析 | 多步骤推理、假设分析、敏感性测算 |
| 多数据源交叉验证 | 同一指标多数据源比对，差异自动标注 |
| 自定义指标口径 | 用户可定义和保存专属指标计算公式 |
| 自动报告生成 | Word/PPT 投行格式报告一键导出 |
| 团队协作 | 查询历史共享、指标库团队共建、知识沉淀 |

**不做**：投资决策自动化、交易执行、监管报送直接替代

