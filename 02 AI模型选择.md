# 02 LLM模型选择

> 本文档为模型选型与分层路由的架构设计。当前实现通过 `.env` 配置单一模型（默认 `Qwen/Qwen3-32B`），各模块共用该模型。分模块多模型路由为后续扩展方向。

#### 1 项目LLM模块使用情况

本项目使用LLM的模块及模块对应的功能如下表所示。

| 模块名称                                    | 核心功能                                                     | 能力需求（LLM角度）                                          |
| ------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **上下文管理模块**                          | 维护多轮对话状态、历史继承、挂起澄清上下文                   | - 多轮上下文理解<br />- 追问与新问题区分<br />- 槽位继承与补全<br />- 状态管理能力 |
| **意图识别与问题重写模块**                  | 识别趋势/对比/排名/同比/归因/图表/投资建议/安全风险等意图，补全槽位 | - 高语义理解<br />- 多类别意图识别<br />- 口语化问题解析<br />- 缺失信息补全能力 |
| **槽位抽取 + Query IR 生成 + SQL 生成模块** | 将自然语言查询结构化为中间表示，约束 API/SQL 调用            | - 精确槽位抽取<br />- Query IR 构建<br />- SQL生成约束<br />- 结构化信息映射能力 |
| **任务规划模块**                            | 拆解复合问题为子任务，识别串并行依赖                         | - 多意图拆解能力<br />- 串行/并行依赖判断<br />- 子任务优先级规划<br />- 执行顺序设计 |
| **数据分析模块**                            | 分析结构化数据和 RAG 证据，生成自然语言结论                  | - 高质量文本生成<br />- 证据引用与整合<br />- 数据事实与归因逻辑结合<br />- 可解释性输出 |
| **异常修复模块**                            | 在 API/SQL 执行失败时生成修复策略或自动重试                  | - 错误诊断推理能力<br />- 生成修复建议<br />- 低延迟决策能力 |

#### 2 分模块LLM候选池

针对项目中不同功能模块（如问题理解、任务规划、分析生成等），整理了可通过 SiliconFlow API 调用的候选大模型列表。

候选模型的推荐基于对各模块功能需求的分析和模型能力特性的匹配，具体待定模型将在后续实验和性能评估中确定。

| 模块名称                                    | 候选大模型                                      |
| ------------------------------------------- | ------------------------------------------------------------ |
| **上下文管理模块**                          | Qwen3-8B<br />Qwen2.5-7B-Instruct<br />DeepSeek-V3-0324 |
| **意图识别与问题重写模块**                  | Qwen3-32B<br />Qwen3-235B-A22B<br />DeepSeek-V3-0324 |
| **槽位抽取 + Query IR 生成 + SQL 生成模块** | Qwen3-32B<br />Qwen2.5-32B-Instruct<br />DeepSeek-V3-0324 |
| **任务规划模块（高复杂度调用）**            | DeepSeek-R1-Distill-Qwen-14B<br />Qwen3-235B-A22B<br />Qwen3-32B |
| **任务规划模块（中等复杂度调用）**          | Qwen3-32B<br />Qwen2.5-32B-Instruct<br />DeepSeek-V3-0324 |
| **数据分析模块（高质量输出）**              | Qwen3-32B<br />DeepSeek-V3-0324<br />Qwen3-235B-A22B |
| **数据分析模块（成本敏感输出）**            | Qwen3-8B<br />Qwen2.5-7B-Instruct |
| **异常修复模块**                            | Qwen3-8B<br />Qwen2.5-7B-Instruct<br />DeepSeek-R1-Distill-Qwen-14B |

#### 3 分模块模型评估方案

本章节用于制定不同 LLM 模块的模型评估方案。FinSafe-QA 由上下文管理、意图识别、Query IR、任务规划、数据分析和异常修复等多个 AI 节点组成，因此模型评估需要按模块分别验证其输入输出是否稳定、是否可控、是否适合进入下游执行链路。

本项目采用“分模块离线评测 + 端到端链路回归”的方式进行模型选择。每个候选模型需要在对应模块评测集中完成同一批 case，输出结构化结果，再按准确性、完整性、稳定性、成本和延迟等指标进行横向比较。最终模型选择根据模块任务特点选择最合适的模型组合。

##### 3.1 上下文管理模块

###### 3.1.1 评估目的

上下文管理模块的核心目标是判断模型能否在多轮财报问答中正确识别“追问”与“新问题”，并合理继承上一轮的公司、指标、报告期、分析动作和澄清状态。该模块直接影响多轮对话体验，如果上下文继承错误，后续 Query IR、SQL/API 查询和分析结论都会被带偏。

因此，本模块评估重点验证模型能否稳定输出可被系统消费的上下文状态更新结果。

###### 3.1.2 评估指标

| 评估指标 | 指标含义 | 评估方式 |
| :--- | :--- | :--- |
| 追问识别准确率 | 判断当前用户问题是否依赖上一轮上下文的准确程度。例如“那净利润呢”“画个图看看”应识别为追问，“云南白药 2024 年营收”应识别为新问题 | 人工标注每条 case 的对话类型，对比模型输出 |
| 上下文继承准确率 | 模型从历史对话中继承公司、指标、时间、对象集合等槽位是否正确 | 对比标准答案中的继承槽位 |
| 上下文覆盖率 | 在需要继承多个槽位时，模型是否完整继承，不遗漏关键字段 | 统计应继承槽位中被正确填充的比例 |
| 新旧话题切换准确率 | 当用户开启新问题时，模型是否能停止继承旧上下文，避免过度继承 | 评估新问题 case 中错误继承的比例 |
| 澄清状态保持率 | 当上一轮处于澄清挂起状态时，模型是否能正确识别用户补充信息并恢复任务 | 使用澄清类多轮 case 评估 |
| 输出结构有效率 | 模型输出是否符合上下文状态 JSON / Schema，能否被下游解析 | 自动校验输出字段、类型和必填项 |
| 平均响应延迟 | 单次上下文判断耗时，影响多轮交互流畅性 | 记录接口平均响应时间和 P95 响应时间 |

###### 3.1.3 通过建议

上下文管理模块建议优先选择稳定、低延迟、结构化输出能力强的中小模型。通过标准建议为：追问识别准确率 ≥ 90%，上下文继承准确率 ≥ 85%，输出结构有效率 ≥ 98%，平均响应延迟 ≤ 2 秒。

##### 3.2 意图识别与问题重写模块

###### 3.2.1 评估目的

意图识别与问题重写模块负责将用户口语化、模糊化、复合型问题转化为清晰、可执行的标准问题表达，并识别查询、趋势、对比、排名、同比、归因、可视化、安全合规等意图。该模块位于系统链路前段，是后续任务规划和 Query IR 生成的基础。

本模块评估目标是验证模型是否能准确理解用户真实需求，并在不改变原意的前提下补全必要上下文、消除表达歧义、输出标准化问题。

###### 3.2.2 评估指标

| 评估指标 | 指标含义 | 评估方式 |
| :--- | :--- | :--- |
| 主意图识别准确率 | 模型识别用户核心任务类型是否正确，如基础问数、趋势分析、公司对比、排名筛选、原因归因 | 对比人工标注的主意图 |
| 多意图召回率 | 复合问题中多个意图是否被完整识别，例如“排名 + 同比 + 画图”不能只识别排名 | 统计标准意图集合中被命中的比例 |
| 多意图精确率 | 模型识别出的意图中有多少是真实存在的，避免过度拆解或误判 | 统计模型输出意图的正确比例 |
| 问题重写忠实度 | 重写后的问题是否保持用户原意，没有增加未提及公司、指标、报告期或结论倾向 | 人工检查或规则比对关键槽位 |
| 问题重写可执行率 | 重写后的问题是否足够清晰，能进入槽位抽取和 Query IR 生成 | 判断是否包含必要任务信息 |
| 模糊问题识别率 | 对“利润怎么样”“最新收入呢”等缺公司、缺时间或口径不清的问题是否触发澄清 | 对比模糊 case 标注 |
| 安全意图识别率 | 是否能识别买卖建议、股价预测、收益承诺、Prompt 注入等高风险意图 | 使用安全合规 case 测试 |
| 输出格式有效率 | 是否按预设 JSON / Schema 输出意图、重写问题、风险标签等字段 | 自动校验结构化输出 |

###### 3.2.3 通过建议

意图识别与问题重写模块建议选择语义理解能力强、分类稳定、结构化输出可靠的模型。通过标准建议为：主意图识别准确率 ≥ 90%，多意图召回率 ≥ 85%，问题重写忠实度 ≥ 90%，安全意图识别率 ≥ 95%。

##### 3.3 槽位抽取 + Query IR 生成 + SQL 生成模块

###### 3.3.1 评估目的

该模块负责把自然语言问题转化为可执行的结构化查询表达，包括公司实体、股票代码、报告期、财务指标、指标口径、排序条件、聚合方式、同比/环比计算逻辑，以及最终 API 参数或只读 SQL。它是 FinSafe-QA 中最需要准确性和可控性的模块。

本模块评估目标是验证模型能否稳定生成正确、可解析、可执行、无高危操作的 Query IR 和 SQL/API 参数，降低字段幻觉、口径误解和查询失败风险。

###### 3.3.2 评估指标

| 评估指标 | 指标含义 | 评估方式 |
| :--- | :--- | :--- |
| 公司实体抽取准确率 | 是否正确识别公司简称、全称、股票代码，并映射为标准公司实体 | 对比标准实体字典 |
| 指标口径映射准确率 | 是否正确区分营业收入、主营业务收入、净利润、归母净利润、利润总额、现金流等指标 | 对比指标字典和标准字段 |
| 报告期识别准确率 | 是否正确识别 2025Q3、2024A、近三年、最新一期等时间表达 | 对比标准报告期 |
| 槽位完整率 | Query IR 中公司、指标、时间、任务类型、过滤条件等必要字段是否完整 | 自动检查必填字段 |
| Query IR 有效率 | 输出的 Query IR 是否符合 Schema、字段类型正确、可被下游执行器解析 | Schema 校验 |
| SQL/API 参数正确率 | 生成的 SQL 或 API 参数是否能查询到正确数据，字段、表名、过滤条件和排序方向正确 | 执行查询并对比期望结果 |
| 只读安全通过率 | SQL 是否仅包含 SELECT 查询，未出现 INSERT、UPDATE、DELETE、DROP 等高风险操作 | 静态规则扫描 |
| 字段幻觉率 | 是否生成不存在的表名、字段名、指标字段或公司编码 | 对照数据库 Schema 检查 |
| 查询执行成功率 | SQL/API 调用是否能成功执行并返回结果 | 在测试数据库或模拟 API 上执行 |
| 结果一致性 | 同一问题多次调用生成的 Query IR / SQL 是否稳定一致 | 多次采样对比输出差异 |

###### 3.3.3 通过建议

该模块建议优先选择结构化输出能力强、遵循 Schema 能力强、幻觉率低的模型。通过标准建议为：Query IR 有效率 ≥ 95%，公司实体抽取准确率 ≥ 90%，指标口径映射准确率 ≥ 90%，查询执行成功率 ≥ 85%，只读安全通过率 = 100%。

##### 3.4 任务规划模块

任务规划模块负责将复合型财报问题拆解为可执行子任务，并判断任务之间的依赖关系、执行顺序和串并行调度方式。该模块不直接产生最终答案，但会决定系统是否能正确调用结构化数据库、RAG、计算器、图表工具和 FinSafe-QA 校验器。

###### 3.4.1 高复杂度调用

**评估目的**

高复杂度调用面向多意图、多步骤、跨数据源、强依赖的复杂问题，例如“找出 2025 年三季度营收最高的医药公司，再比较其近三年净利润同比，并解释增长原因”。这类问题需要模型先拆任务，再确定哪些任务可并行、哪些必须串行。

**评估指标**

| 评估指标 | 指标含义 | 评估方式 |
| :--- | :--- | :--- |
| 子任务拆解完整率 | 是否覆盖用户问题中的全部子任务，如排名、筛选、同比、趋势、归因、可视化 | 对比标准任务列表 |
| 子任务拆解准确率 | 拆出的任务是否必要且正确，没有遗漏、重复或无关任务 | 人工或规则评估任务合理性 |
| 依赖关系正确率 | 是否正确判断任务前后依赖，例如归因必须依赖数据查询结果，图表必须依赖可绘制数据 | 对比标准 DAG |
| 串并行调度正确率 | 是否能把实体识别、指标映射、RAG 检索等可并行任务与数据依赖任务正确区分 | 对比标准执行批次 |
| 工具选择准确率 | 是否为每个子任务选择正确工具，如数据库查询、RAG 检索、计算、图表生成 | 对比标准工具调用 |
| 任务优先级合理性 | 是否先执行必要查询和校验，再生成分析结论，避免先结论后查数 | 人工审查执行计划 |
| 计划可执行率 | 输出任务计划是否能被调度器解析和执行 | Schema 校验 + 执行模拟 |
| 复杂任务完成率 | 按该计划执行后，是否能得到完整可用结果 | 端到端执行评估 |

**通过建议**

高复杂度任务规划建议使用推理能力更强的模型。通过标准建议为：子任务拆解完整率 ≥ 85%，依赖关系正确率 ≥ 90%，计划可执行率 ≥ 95%，复杂任务完成率 ≥ 80%。

###### 3.4.2 中等复杂度调用

**评估目的**

中等复杂度调用面向常见的趋势、对比、同比、排名和基础可视化任务。这类任务要求响应速度更快、成本更低，同时保持稳定的任务拆解和工具调用能力。

**评估指标**

| 评估指标 | 指标含义 | 评估方式 |
| :--- | :--- | :--- |
| 常见任务识别准确率 | 是否能识别趋势、对比、排名、同比、图表等常见任务 | 对比人工标注 |
| 任务拆解简洁度 | 是否避免过度拆解，保持执行计划足够简单 | 统计无关子任务比例 |
| 工具调用路径正确率 | 是否选择正确执行链路，如“趋势问题 → 多期查询 → 折线图” | 对比标准路径 |
| 执行批次合理性 | 是否合理安排串行和并行步骤，减少不必要等待 | 对比标准批次或模拟耗时 |
| 平均规划延迟 | 模型生成任务计划的平均耗时 | 记录平均响应时间和 P95 |
| 单次规划成本 | 单次任务规划消耗的 token 或调用费用 | 记录输入输出 token 和价格 |

**通过建议**

中等复杂度任务规划建议选择成本和速度更优的模型。通过标准建议为：常见任务识别准确率 ≥ 90%，工具调用路径正确率 ≥ 90%，平均规划延迟 ≤ 3 秒，单次规划成本低于高复杂度模型 30% 以上。

##### 3.5 数据分析模块

数据分析模块负责基于结构化财务数据和 RAG 证据生成自然语言结论。该模块需要把“数据事实”“证据观点”“模型总结”区分清楚，不能脱离数据自由发挥，也不能把研报观点包装成确定性事实。

###### 3.5.1 高质量输出

**评估目的**

高质量输出用于复杂归因、横向对比、经营分析和需要较强表达质量的场景。评估重点是模型能否生成准确、完整、可解释、可追溯且符合金融合规边界的分析结论。

**评估指标**

| 评估指标 | 指标含义 | 评估方式 |
| :--- | :--- | :--- |
| 数据事实准确率 | 回答中的公司、报告期、指标、数值、单位、同比和排序结论是否与查询结果一致 | 对比结构化数据结果 |
| 证据引用有效率 | 归因结论是否引用了相关公告、研报、政策或数据证据 | 检查引用是否命中证据片段 |
| 归因忠实度 | 原因分析是否严格基于召回证据，没有无依据推断或夸大 | 人工评估或证据一致性检查 |
| 结论完整性 | 是否覆盖用户全部问题，包括数据结果、趋势判断、原因解释、风险提示等 | 对照标准答案要点 |
| 表达清晰度 | 输出是否易读，层次清楚，避免过度冗长或术语堆砌 | 人工评分 |
| 来源可追溯性 | 是否清楚标注数据来源、报告期、证据来源和口径说明 | 检查来源字段 |
| 合规通过率 | 是否避免买卖建议、目标价、确定收益承诺、未披露信息等风险输出 | 安全规则检查 |
| 幻觉率 | 是否编造财务数据、研报观点、公告内容或不存在的来源 | 人工复核 + 来源校验 |

**通过建议**

高质量数据分析建议使用表达和推理能力较强的模型。通过标准建议为：数据事实准确率 ≥ 95%，证据引用有效率 ≥ 85%，归因忠实度 ≥ 90%，合规通过率 ≥ 98%，幻觉率 ≤ 2%。

###### 3.5.2 成本敏感输出

**评估目的**

成本敏感输出用于基础问数、简单趋势、单指标解释等高频低复杂度场景。该模块不追求长文本分析，而是要求快速、稳定、低成本地生成简洁答案。

**评估指标**

| 评估指标 | 指标含义 | 评估方式 |
| :--- | :--- | :--- |
| 简答准确率 | 简短回答中的核心数值、单位、公司和报告期是否正确 | 对比结构化查询结果 |
| 输出简洁度 | 是否避免不必要扩写，能用较短文本回答基础问题 | 统计输出字数和无关信息比例 |
| 必要口径说明率 | 对收入、利润、现金流等易混指标是否给出必要口径说明 | 检查是否包含口径字段 |
| 平均响应延迟 | 从输入数据到生成答案的平均耗时 | 记录平均响应时间和 P95 |
| 单次生成成本 | 单次回答消耗的 token 或调用费用 | 记录 token 和价格 |
| 格式稳定性 | 是否稳定输出约定格式，如“结论 + 数据来源 + 风险提示” | 自动检查格式字段 |

**通过建议**

成本敏感输出建议选择速度快、价格低、结构化表达稳定的模型。通过标准建议为：简答准确率 ≥ 95%，平均响应延迟 ≤ 2 秒，单次生成成本低于高质量输出模型 50% 以上，格式稳定性 ≥ 95%。

##### 3.6 异常修复模块

###### 3.6.1 评估目的

异常修复模块负责在 SQL/API 执行失败、字段不存在、参数缺失、结果为空、结果形态异常或证据不足时，判断错误类型并给出修复策略。该模块的目标不是“强行回答”，而是在可修复时安全重试，在不可修复时触发澄清、部分回答或保守拒答。

###### 3.6.2 评估指标

| 评估指标 | 指标含义 | 评估方式 |
| :--- | :--- | :--- |
| 错误类型识别准确率 | 是否正确识别字段错误、表名错误、参数缺失、权限失败、结果为空、证据不足等异常类型 | 对比人工标注错误类型 |
| 修复策略准确率 | 是否能给出正确修复动作，如替换字段、补充参数、调整报告期、降级回答或触发澄清 | 对比标准修复策略 |
| 自动修复成功率 | 按模型修复建议重试后，查询是否成功执行并返回有效结果 | 执行修复后的 SQL/API |
| 安全退出率 | 不可修复时是否能停止重试并输出安全兜底，而不是编造结果 | 异常 case 人工复核 |
| 最大重试控制率 | 是否遵守最大重试次数，避免死循环或无限调用 | 调度日志检查 |
| 低可信识别率 | 是否能识别数据不足、证据不足、口径冲突等低可信情况 | 对比低可信 case 标注 |
| 修复延迟 | 异常诊断和修复生成耗时 | 记录平均响应时间和 P95 |
| 修复成本 | 异常修复单次调用 token 或费用 | 记录 token 和价格 |

###### 3.6.3 通过建议

异常修复模块建议选择错误诊断能力强、低延迟、遵循规则稳定的模型。通过标准建议为：错误类型识别准确率 ≥ 85%，修复策略准确率 ≥ 80%，安全退出率 ≥ 95%，最大重试控制率 = 100%。

#### 4 分模块模型评估结果

本轮评估基于 `evaluation/模型选择分模块测试数据集.xlsx` 设计分模块测试集，覆盖上下文管理、意图识别与问题重写、槽位抽取 / Query IR / SQL 生成、任务规划、数据分析 5 个核心 LLM 模块。按照本项目当前范围，暂不进行第 6 个模块“异常修复模块”的测评。

本轮评估重点不追求大规模压测，而是通过小样本抽样评估验证模型选择思路：不同模块的模型能力需求不同，因此不应简单使用单一模型覆盖全部链路，而应根据任务复杂度、结构化输出要求、推理深度、响应速度和调用成本进行分层选择。

##### 4.1 评测数据集概览

| 模块 | 测试用例数 | 标准答案字段 | 本轮是否评测 |
| :--- | ---: | :--- | :--- |
| 上下文管理模块 | 83 | 是否追问、继承槽位、澄清状态 | 是 |
| 意图识别与问题重写模块 | 77 | 主意图、附加意图、重写问题文本、安全标记 | 是 |
| 槽位抽取 + Query IR + SQL 生成模块 | 212 | 公司实体、指标、报告期、过滤条件/排序/聚合、生成 SQL/API | 是 |
| 任务规划模块 | 301 | 子任务列表、串/并行依赖、工具选择 | 是 |
| 数据分析模块 | 83 | 数据事实、证据引用、归因分析结论、合规性标记 | 是 |
| 异常修复模块 | - | 错误类型、修复策略、重试结果 | 否，本轮不测 |

##### 4.2 评估方式

本轮采用“分模块抽样评估 + 能力匹配分析”的方式进行模型选择：

1. 每个模块选取典型 case，覆盖基础、复杂、边界和安全场景；
2. 要求模型按照模块输出 Schema 返回结构化结果；
3. 将模型输出与测试集中标准字段进行比对；
4. 对每个模块分别观察准确性、完整性、结构化稳定性、成本和响应速度；
5. 根据模块能力需求选择不同模型，而不是只看单一总分。

##### 4.3 候选模型范围

本轮测试通过硅基流动 Chat Completions 接口调用候选模型。考虑到本项目需要兼顾结构化输出、推理能力、成本和响应速度，候选池选择如下：

| 候选模型 | 主要定位 | 适合重点观察的能力 |
| :--- | :--- | :--- |
| `Qwen/Qwen3-8B` | 成本敏感、低延迟模型 | 上下文管理、意图识别、基础问题重写、简短回答生成 |
| `Qwen/Qwen3-32B` | 综合能力模型 | Query IR 生成、任务规划、结构化输出、通用分析生成 |
| `deepseek-ai/DeepSeek-R1-Distill-Qwen-14B` | 推理增强模型 | 复杂任务拆解、依赖关系判断、归因逻辑分析 |

##### 4.4 分模块评估结果

| 模块 | 核心观察指标 | 评估发现 | 推荐模型 |
| :--- | :--- | :--- | :--- |
| 上下文管理模块 | 追问识别准确率、继承槽位覆盖率、澄清状态准确率、输出结构有效率 | 该模块任务边界清晰，主要判断是否追问、继承哪些槽位、是否需要继续澄清，对深度推理要求不高，但要求响应快、格式稳定。轻量模型即可满足该模块使用需求 | `Qwen/Qwen3-8B` |
| 意图识别与问题重写模块 | 主意图识别准确率、附加意图召回率、问题重写可执行率、安全标记准确率 | 该模块需要同时识别趋势、对比、排名、同比、归因、图表、安全等多类意图，还要避免重写时改变用户原意。相比轻量模型，综合能力模型在多意图召回和重写稳定性上更合适 | `Qwen/Qwen3-32B` |
| 槽位抽取 + Query IR + SQL 生成模块 | 公司实体准确率、指标口径准确率、报告期准确率、查询逻辑准确率、只读安全通过率 | 该模块是问数链路中风险最高的环节，错误会直接导致查错数或 SQL/API 调用失败。评估中重点关注 Schema 遵循、字段稳定性和只读安全，因此优先选择结构化输出更稳的综合模型 | `Qwen/Qwen3-32B` |
| 任务规划模块：中等复杂度调用 | 子任务拆解覆盖率、依赖关系准确率、工具选择准确率、计划可执行率 | 对趋势、对比、排名、同比、图表等常规问题，综合模型可以稳定拆出数据查询、计算、图表生成和结果校验任务，成本和质量较均衡 | `Qwen/Qwen3-32B` |
| 任务规划模块：高复杂度调用 | 多意图拆解能力、串并行依赖判断、工具调用顺序、复杂任务完成率 | 对“先筛选排名，再做同比，再解释原因并画图”这类复合问题，模型需要更强的推理和依赖分析能力，因此适合使用推理增强模型处理高复杂度规划 | `deepseek-ai/DeepSeek-R1-Distill-Qwen-14B` |
| 数据分析模块：高质量输出 | 数据事实准确率、证据引用有效率、归因忠实度、合规标记准确率 | 高质量输出需要把结构化数据、RAG 证据和自然语言结论结合起来，并区分数据事实、证据观点和模型总结。综合模型在表达质量、证据整合和合规边界上更适合作为默认生成模型 | `Qwen/Qwen3-32B` |
| 数据分析模块：成本敏感输出 | 简答准确率、输出简洁度、平均响应延迟、单次生成成本 | 基础问数和简单趋势类问题不需要复杂长文本生成，轻量模型可以承担短回答生成任务，用于降低高频调用成本 | `Qwen/Qwen3-8B` |

##### 4.5 评估结论

本轮模型评估的核心结论是：FinSafe-QA 不适合把所有任务都交给同一个模型处理。不同模块的失败成本不同，所需能力也不同：

- 上下文管理和简单回答属于高频轻任务，应优先选择低延迟、低成本模型；
- Query IR、SQL/API 生成、意图重写属于链路关键节点，应优先选择结构化输出稳定的综合模型；
- 复杂任务规划和多步骤依赖分析属于推理密集型任务，应在必要时调用推理增强模型；
- 数据分析模块需要兼顾事实准确、证据引用和表达质量，因此默认使用综合模型，简单场景可降级到轻量模型。

因此，本项目最终采用分层模型路由方案：**轻量模型处理高频简单任务，主力模型处理核心结构化任务，推理增强模型处理高复杂度任务。**

#### 5 项目模型选择

基于第 1 节的模块能力需求、第 3 节的评估方案，以及第 4 节的数据集设计，本项目模型选择采用“分模块路由 + 成本分层 + 高风险兜底”的策略，而不是所有节点统一使用同一个大模型。

##### 5.1 模型选择原则

| 原则 | 说明 |
| :--- | :--- |
| 高准确模块优先稳定性 | Query IR、SQL/API、任务规划等模块一旦出错会影响整个链路，因此优先选择结构化输出稳定、字段幻觉低的模型 |
| 高频轻任务优先低成本 | 上下文判断、基础意图识别、简短回答等调用频次高，优先选择低延迟、低成本模型 |
| 复杂推理任务优先推理能力 | 多意图任务规划、归因分析、跨数据源解释等任务优先选择推理能力更强的模型 |
| 金融场景优先保守输出 | 所有模型选择必须满足不编造数据、不输出投资建议、不泄露敏感信息的安全边界 |
| 支持动态降级与升级 | 当低成本模型置信度不足时，可升级到更强模型；当复杂模型成本过高时，简单任务回落到轻量模型 |

##### 5.2 模型路由方案

项目采用以下模型路由方案：

| 模块 | 选择模型 | 选择理由 |
| :--- | :--- | :--- |
| 上下文管理模块 | `Qwen/Qwen3-8B` | 该模块输出字段较少，主要判断追问、新问题、槽位继承，适合使用低成本、低延迟模型；若多轮继承错误率高，则升级到 `Qwen/Qwen3-32B` |
| 意图识别与问题重写模块 | `Qwen/Qwen3-32B` | 需要识别多意图、模糊问题、安全风险，并保持重写忠实度，建议使用综合能力更稳的模型 |
| 槽位抽取 + Query IR + SQL 生成模块 | `Qwen/Qwen3-32B` | 该模块要求严格遵循 Schema、准确映射公司/指标/报告期，并保证 SQL/API 只读安全，优先选择结构化输出更稳定的模型 |
| 任务规划模块：高复杂度调用 | `deepseek-ai/DeepSeek-R1-Distill-Qwen-14B` | 高复杂度问题需要拆解多意图、判断依赖关系、设计串并行执行顺序，优先使用推理增强模型 |
| 任务规划模块：中等复杂度调用 | `Qwen/Qwen3-32B` | 趋势、同比、排名、可视化等常规任务需要稳定规划和较快响应，综合模型更均衡 |
| 数据分析模块：高质量输出 | `Qwen/Qwen3-32B` | 财务数据分析需要表达清晰、证据引用稳定、合规边界清楚，综合模型更适合作为默认分析生成模型 |
| 数据分析模块：成本敏感输出 | `Qwen/Qwen3-8B` | 基础问数、简单趋势、短回答场景可使用轻量模型降低调用成本 |

##### 5.3 最终模型组合

最终采用以下组合：

| 调用层级 | 使用场景 | 推荐模型 |
| :--- | :--- | :--- |
| 轻量模型层 | 上下文管理、简单意图判断、基础问数短回答 | `Qwen/Qwen3-8B` |
| 主力模型层 | 意图识别与问题重写、Query IR、SQL/API 生成、中等复杂度任务规划、默认数据分析 | `Qwen/Qwen3-32B` |
| 推理增强层 | 高复杂度任务规划、复杂归因、多步骤依赖分析 | `deepseek-ai/DeepSeek-R1-Distill-Qwen-14B` |

##### 5.4 路由与降级策略

为了兼顾效果、成本和稳定性，系统在实际调用中采用以下路由与降级规则：

| 场景 | 路由规则 |
| :--- | :--- |
| 高频简单问数 | 由轻量模型生成简短回答，降低响应延迟和调用成本 |
| 槽位不完整或口径不清 | 先由上下文管理 / 意图识别模块触发澄清，不直接进入 SQL/API 查询 |
| Query IR 或 SQL/API 生成 | 固定使用主力模型，并对输出做 Schema 校验和只读安全检查 |
| 复合任务规划 | 中等复杂度任务使用主力模型，高复杂度任务升级到推理增强模型 |
| 归因分析 | 必须先完成结构化数据查询和 RAG 证据召回，再进入数据分析模块 |
| 低可信结果 | 不升级生成结论，而是触发风险提示、部分回答或保守拒答 |

##### 5.5 最终结论

本项目最终选择 **`Qwen/Qwen3-32B` 作为主力模型，`Qwen/Qwen3-8B` 作为轻量高频模型，`deepseek-ai/DeepSeek-R1-Distill-Qwen-14B` 作为复杂规划与推理增强模型**。

该组合适合 FinSafe-QA 的业务特点：财报问数场景既需要低成本快速响应，也需要在关键链路上保证结构化输出稳定和金融结果可信。通过分模块路由，系统可以把不同模型放在最适合的位置，而不是用一个大模型承担所有任务，从而在准确性、成本、响应速度和可控性之间取得更好的平衡。
