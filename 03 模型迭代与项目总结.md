# 03 模型迭代与项目总结

## 1. 三版本模型策略演进

### 1.1 V1.0 — 单模型 + 白名单 SQL

V1.0 的核心目标是"可信问数"，模型策略以安全可控为优先。

- **模型角色**：自然语言意图解析 + 结果表达
- **提示词层**：`INTENT_SYSTEM_PROMPT`（意图识别 JSON）+ `ANSWER_SYSTEM_PROMPT`（结论生成）
- **SQL 生成**：系统基于白名单表/字段模板生成，模型不直接拼接 SQL
- **兜底策略**：LLM 不可用时自动回退到规则解析
- **默认模型**：Qwen/Qwen2.5-7B-Instruct

```
用户问题 → 大模型解析意图 JSON → 系统模板生成 SQL → 执行 → 模板生成答案
```

V1.0 的核心原则：大模型负责"理解"，系统负责"查询"。

### 1.2 V2.0 — 提示词分层 + 规则计算

V2.0 在 V1.0 可信问数基础上增加分析和可视化，模型策略从单层扩展到三层提示词。

- **模型角色**：分析意图识别 + 洞察生成 + 图表校验
- **提示词层**：
  - `ANALYSIS_INTENT_SYSTEM_PROMPT` — 分析类型、指标、图表推荐
  - `INSIGHT_SYSTEM_PROMPT` — 基于计算结果的克制分析结论
  - `CHART_REVIEW_PROMPT` — 图表类型/字段/单位合理性校验
- **计算逻辑**：排名、占比、增长率、研发效率等由规则层完成，模型只做表达
- **默认模型**：Qwen/Qwen3-32B（主力模型升级）

```
用户问题 → 分析意图 JSON → SQL + 规则计算 → 洞察生成 + 图表配置 → 校验输出
```

V2.0 的核心原则：模型辅助理解，系统负责计算，结果必须可追溯。

### 1.3 V3.0 — 上下文管理 + 任务规划（代码层）

V3.0 的核心变化不在模型层，而在代码层引入上下文管理和任务规划能力。

- **模型角色**：与 V2.0 相同（意图识别 + 洞察生成 + 图表校验）
- **新增代码层能力**：
  - `rewrite_with_context()` — 多轮对话状态、追问识别、槽位继承
  - `build_agent_plan()` — 复合问题拆解、任务类型标记、依赖编排
- **提示词层**：沿用 V2.0 的三层提示词，未新增模型调用
- **默认模型**：Qwen/Qwen3-32B

```
用户问题 → 上下文重写 → 分析意图 JSON → 任务规划拆解 → SQL/计算/边界识别 → 洞察 + 图表 → 输出
```

V3.0 的核心原则：不追求模型能力突破，而是在可信边界内让复杂查询变得可控、可解释、可评测。

### 1.4 三版本模型策略对比

| 维度 | V1.0 | V2.0 | V3.0 |
| --- | --- | --- | --- |
| LLM 调用次数 | 1-2 次 | 2-3 次 | 2-3 次 |
| 提示词层数 | 2 层 | 3 层 | 3 层 |
| 模型数量 | 1 | 1 | 1 |
| 新增能力来源 | 模型 + SQL 模板 | 模型 + 规则计算 | 代码层（上下文/规划） |
| 默认模型 | Qwen2.5-7B | Qwen3-32B | Qwen3-32B |

三版本演进的核心逻辑是：**先把数据查准，再把分析做好，最后让复杂查询可控**。模型始终是"单一大模型 + 结构化约束"模式，新能力更多来自代码层和产品设计，而非模型数量或规模的堆叠。

---

## 2. 当前模型架构现状

### 2.1 实际运行方式

三个版本均通过 SilicoFlow OpenAI-compatible 接口调用单一可配置模型：

```text
SILICONFLOW_MODEL=Qwen/Qwen3-32B
```

所有提示词（意图识别、洞察生成、图表校验）共享同一模型。LLM 不可用时，系统自动回退到规则解析，保证本地运行不依赖外部 API。

### 2.2 与 02 AI模型选择.md 的差异

[02 AI模型选择.md](02%20AI模型选择.md) 描述了一套**分模块多模型分层路由架构**，将系统拆分为 6 个模块（上下文管理、意图识别、槽位抽取+Query IR+SQL 生成、任务规划、数据分析、异常修复），并为每个模块推荐不同量级的候选模型。该文档最终结论为：

> 选择 Qwen/Qwen3-32B 作为主力模型，Qwen/Qwen3-8B 作为轻量高频模型，DeepSeek-R1-Distill-Qwen-14B 作为复杂规划与推理增强模型。

**当前 engine.py 未实现上述分模块多模型路由。** 各版本 engine.py 均为单一模型调用，所有模块共享 `.env` 配置的同一模型。02 AI模型选择.md 是架构设计文档，描述了模型选型的思考过程和演进方向，而非当前代码的实现状态。

### 2.3 设计意图与实现差距

| 02 文档描述 | 当前实现 | 差距 |
| --- | --- | --- |
| 6 模块独立模型选择 | 单一模型覆盖所有模块 | 未实现模块级模型路由 |
| 轻量/主力/推理三层模型 | 仅使用主力模型 | 未实现分层调用 |
| 模块级评估与门禁 | 整体评测 | 未实现分模块质量度量 |
| 成本敏感的模型切换 | 无成本控制逻辑 | 未实现 |

这一差距本身是项目迭代规划的一部分：**先验证单一模型能否在可信约束下完成核心链路，再逐步引入多模型路由优化成本和特定模块的准确率。**

---

## 3. 未来迭代方向

### 3.1 分模块模型路由落地

将 02 AI模型选择.md 中的分层设计逐步落地到 engine.py：

- **轻量模型层**（Qwen3-8B）：接管上下文管理、追问识别等高频低延迟任务
- **主力模型层**（Qwen3-32B）：继续负责意图识别、SQL 生成约束、数据分析
- **推理增强层**（DeepSeek-R1）：处理高复杂度任务规划和归因分析

### 3.2 模型升级的回归保障

- 建立模型升级门禁：切换基座模型或调整提示词后，核心评测集准确率下降不超过 5%
- 自动回归流水线：每次模型变更自动运行评测集并生成对比报告
- Bad Case 库：积累和分类失败案例，作为模型升级和提示词优化的依据

### 3.3 成本与延迟优化

- 按模块复杂度路由到不同量级模型，降低平均 token 消耗
- 高频简单查询（单公司单指标）走轻量模型快速通道
- 对响应延迟、token 消耗、模块成功率建立观测指标

### 3.4 可观测性

- 记录每次 LLM 调用的延迟、token 消耗和输出质量
- 建立模块级成功率看板，追踪各模块在不同模型下的表现
- 异常检测：模型输出偏离预期格式时自动告警

---

## 4. 项目总结

FinSafe-QA 通过三个版本的迭代，验证了一条核心命题：

> 金融 AI 产品的可信度不取决于模型能力有多强，而取决于产品链路是否把模型放在可验证、可约束、可评测的位置。

三个版本分别回答了三个产品问题：

1. **V1.0 可信问数**：自然语言能稳定映射到可审计 SQL 吗？→ 能，通过白名单 Schema + 模板约束。
2. **V2.0 分析与可视化**：在查数可信基础上，能做分析和可视化吗？→ 能，通过规则计算 + 图表配置。
3. **V3.0 上下文与规划**：复杂多步查询能变得可控吗？→ 能，通过上下文管理 + 任务规划编排。

每个版本都不是在"模型能力"上做加法，而是在"产品约束"上做减法——让模型在更狭窄、更可验证的边界内工作，从而提升整体可信度。
